#include <stdio.h>

int main(int argc, char *argv[]) {
    int n;

    do {
        printf("\nEnter an integer: ");
        scanf("%d", &n);
    } while ( n < 1);

    int sum = 0, sum_sqrs = 0, avg = 0;

    int i;

    for (i = 0; i
}
