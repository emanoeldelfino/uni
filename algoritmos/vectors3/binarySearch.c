#include <stdio.h>

#define N 10

int main(int argc, char *argv[]) {
    int found = 0;
    int start = 0, end = N - 1, half;

    do {
        half = (start + end) / 2;
        if (v[half] == key)
    } while (!found && start <= end);
}
